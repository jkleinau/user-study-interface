/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost'], // Add your domain if necessary
  },
    output: "standalone",
  };
  
  export default nextConfig;
  